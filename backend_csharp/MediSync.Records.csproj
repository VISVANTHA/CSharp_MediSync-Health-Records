<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>MediSync.Records</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <Compile Remove="tests\**\*.cs" />
    <Content Remove="tests\**\*" />
    <None Include="tests\**\*" />
  </ItemGroup>
  <!-- MiniCover / Stryker are invoked via run_tools (dotnet tool), not PackageReference -->
</Project>
